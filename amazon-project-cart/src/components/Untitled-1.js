// make a javascript drum kit vanilla js => youtube video

// api ko call karke kese kaam hota he 

/*npm i axios in terminal

axios ko import karna padega
import axios from 'axios';
axios.get = in side the function
api ke data ka response 'promise' ke for ke aata he
promise use krte he asyncronus code ko handle krne ke liye
async/await ko use krte he promise ko handle krne ke liye

const [username,setUsername] = usestate([
{name, password,age}
])

const getDrumKitData = async () => {
    const response = await axos.get('url')
    console.log(response)
    setUserData(response.data)
    }

    button = {setUsername.map((elem)=>console.log(elem.username))}
*/
